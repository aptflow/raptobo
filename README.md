# Raptobo - Rust APT Tool Box

Raptobo offers a library and CLI tools for working with APT repositories.

## TODO

- `Release` file as fallback, if `InRelease` is not available.
- Check signature of `InRelease` / `Release` file.